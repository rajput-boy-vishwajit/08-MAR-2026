-------------------------
GIT COMMANDS IN ONE SHOT
-------------------------
#==to check version and  git is installded or not
git --version


mkdir project
cd project

# ssh-keygen
# public key->copy->Github->settings->deploy key->ssh=>write permission tick...
git init

# configure git as global
git config --global user.name "My Name"
git config --global user.email "someone@gmail.com"

git config user.email <EMAIL>
git config user.name  <USERNAME>

git config --list
 
git remote add origin <link of https repository>
# IF YOU BY MISTAKE ADD THE FALSE ORIGIN
git remote remove origin #((IF YOU WANT TO REMOVE THE ALREADY ADDED ORIGIN))
git remote -v

echo "Hello" | tee index.html

git status

git add .
git commit -m "SOME INITIAL COMMIT MESSAGE"

git push origin main <BRANCH NAME main/master>

--------------------------------
SOME IMPORTANT COMMANDS TO KNOW
--------------------------------
git branch 
git branch -M <Branch name Change?> 

===================================
##== - CREATE A FOLDER IN VS CODE
# GITDEMO
cd GITDEMO
git --version

## CLONE & STATUS
git clone <SOME LINK FROM GITHUB HTTPS/SSH>
git status
## -GitHub repo code directly cloned downloaded into the folder (it shows subfolder, code, readme.MD)
## -some code changes in local code 
git status
# - modified :
git commit -m "

# UNTRACKED - New files that git dosen't yet track
# MODIFIED - Changed
# STAGED   - File is ready to be committed
# UNCHANGED - Unchanged

## ADD & COMMIT 
# ADD - Adds new or changed files in your working directory to the Git staging area.
git add <FILE NAME>

# COMMIT - it is the record of change
git commit -m "SOME MESSAGE"

## PUSH COMMAND
# PUSH - Upload local repo contect to remote repo
git push origin main


## INIT COMMAND 
# init - used to create a new git repo
# create a new repo with name - localrepo
git init
git remote add origin <GITHUB SSH LINK>
git remote -v <TO VERITY REMOTE>
git branch <TO CHECK BRANCH>
git branch -M main <to rename branch>
git push origin main


cd ..
mkdir LocalRepo
cd LocalRepo
ls -la
git init
ls -la
git status
git add .
git status
git commit -m "Add initial Files"
git status

git branch
# *master
git branch -M main
# *main - rename the branch

git push origin main
git status


git add .
git commit -m "Add Readme"

## WORKFLOW - Local Git > GitHub repo >> clone >> changes >> add >> commit >> push.


## GIT BRANCHES :
# feature 
# master

git branch					--- (TO CHECK BRANCH)
git branch -M main				--- (TO RENAME BRANCH)
git checkout  <BRANCH NAME WHERE TO GO>		--- (TO NEVIGATE BRANCH)
git checkout -b <NEW BRANCH NAME CREATE AND GO> --- (TO CREATE NEW BRANCH)
git branch -d <BRANCH NAME>			--- (TO DELETE BRANCH)


git branch
# on branch *main
git checkout -b feature1
# switched to a new branch *feature1
git branch
# *feature1
#  main
git checkout main
# switched to branch "main"
# feature
# *main

git checkout -b feature2
# swithched to a new branch *feature2
git checkout main
# we have to chenge another branch to delete
git branch -d feature2
# Deleted branch feature2

# Adding changes in index.html <p>This is a new feature</p>
git branch
# *feature1
git status
# midified: index.html
git commit -m "Add new feature"
git status
git checkout main
git checkout feature1

## MERGING CODE :
#----------------
# WAY 1:
git diff <BRANCH NAME>		--- (TO COMPARE COMMITS, BRANCHES, FILES & MORE)
git merge <BRANCH NAME>		--- (TO MERGE 2 BRANCHES)

# WAY 2:
# CREATE A PR


## GIT MERGE CONFLICTS :
# ----------------------
git branch
git diff main
# Shows changed sentence also
git merge main



## PULL REQUEST : It lets you tell others about changes you're pushed to a branch in a repository on GitHub.

## PULL COMMAND : Used to fetch and download content from a remote repo and immediately update the local repo to match that content.
git checkout main
git branch	--- ( *main )

git pull origin main		--- ( This pull the code from GitHub and merge with local Branch )


## RESOLVING MERGE CONFLICTS :
#-----------------------------
# - An event that takes place when Git is unable to automatically resolve differences in code between two commits.
# GITDEMO
#  >LocalRepo
#   >index.html
#   >README.md



## UNDOING CHANGES :
#-------------------
# CASE 1: Staged changes
git reset <FILE NAME>
git reset

# CASE 2: Commited changes (for one commit)
git reset HEAD~1

# CASE 3: Commited changes (for many commits)
git reset <COMMIT HASH>
git reset --hard <COMMIT HASH>

